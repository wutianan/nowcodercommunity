<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fz.newcoder.community.dao.CommentMapper">

    <sql id="selectFields">
        id, user_Id,entity_type,entity_id,target_Id,content,status,create_time
    </sql>

    <sql id="insertFields">
        userId,entityType,entityId,targetId,content,status,createTime
    </sql>

    <select id="selectCommentByEntity" resultType="com.fz.newcoder.community.entity.Comment">
        select <include refid="selectFields"></include>
        from newcodercommnuity.comment
        where status = 0
        and entity_type = #{entityType}
        and entity_id = #{entityId}
        order by create_time asc
        limit #{offset},#{limit}
    </select>

    <select id="selectCountByEntity" resultType="java.lang.Integer">
        select count(id) from newcodercommnuity.comment
        where status = 0
        and entity_type = #{entityType}
        and entity_id = #{entityId}
    </select>


</mapper>